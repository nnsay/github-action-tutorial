name: Test self host runner

on: workflow_dispatch

jobs:
  shell:
    name: self host runner
    runs-on: demo
    steps:
      - name: check env
        run: env

      - name: check command
        run: |
          jq --version || true
          aws --version || true
          gh --version || true
