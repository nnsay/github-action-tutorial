name: Dev(Stagign) release

on:
  push:
    branches:
      - release*

jobs:
  deploy-aws:
    uses: ./.github/workflows/deploy-aws.yml
    with:
      envName: staging
      workflowId: 60653738
    secrets: inherit
    name: Deploy AWS - Staging environment
